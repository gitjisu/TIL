1.

```
1) F ( Single Page Application )
2) T 
3) T
4) F 
5) F (vind = : on = @)
6) F (양방향 데이터 바인딩)
```

2.

```
MVVM은 Model, Viwe, View Model의 약자이다.
Model : JS Object로 data로 존재하며 data가 바뀌면 DOM이 반응한다
View : HTML
View Model : View와 Model 사이에서 data와 DOM에 관련된 모든일을 처리한다
```

3.

```
a) message
b) new Vue
c) '#app'
```

